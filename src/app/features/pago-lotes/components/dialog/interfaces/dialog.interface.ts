export interface TableDialog {
    label: string;
    icon?: string;
    roudend: boolean;
    isSearch: boolean;
    title: string;
}

export interface Column {
    field: string;
    header: string;
}
